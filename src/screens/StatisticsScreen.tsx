import { Text, SafeAreaView } from 'react-native';

export const StatisticsScreen = () => {
    return (
        <SafeAreaView>
            <Text>StatisticsScreen</Text>
        </SafeAreaView>
    );
};
