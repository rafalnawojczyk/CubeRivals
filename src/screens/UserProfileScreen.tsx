import { Text, SafeAreaView } from 'react-native';

export const UserProfileScreen = () => {
    return (
        <SafeAreaView>
            <Text>UserProfileScreen</Text>
        </SafeAreaView>
    );
};
