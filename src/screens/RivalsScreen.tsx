import { SafeAreaView, Text } from 'react-native';

export const RivalsScreen = () => {
    return (
        <SafeAreaView>
            <Text style={{ fontSize: 24 }}>Text</Text>
        </SafeAreaView>
    );
};
